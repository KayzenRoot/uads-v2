# adapters

Host adapter documentation for the fixed Prompt 010 registry:
`cursor`, `codex`, and `generic-agent-skills`. All three share one
provider-neutral contract, global ownership model, conservative capability
provenance, and sidecar-only Host Dispatch Bundle preparation.

UADS by NexLabs. See `docs/` for Architecture Freeze v0.2.

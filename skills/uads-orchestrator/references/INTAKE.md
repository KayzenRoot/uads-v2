# Intake

Create provider-neutral JSON matching `schemas/intake.schema.json`.

Include objective, constraints, in/out of scope, acceptance criteria, domainSignals, riskSignals, destructiveSignals, affectedAreas, uncertainties, and approvedBoundaries.

Do not store chain-of-thought. Store operational conclusions only.

Preferred invocation: `uads plan --intake intake.json`.

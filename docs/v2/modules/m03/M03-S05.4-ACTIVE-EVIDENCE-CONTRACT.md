# M03 S05.4 — Active Evidence Contract & PCCR 1.1

Status: FROZEN BEFORE IMPLEMENTATION
Work Order: UADS2-WO-009
Issue: #34

## PCCR 1.1

Backward-compatible reader:
- accepts valid 1.0 records;
- accepts 1.1 records;
- emits 1.1 for newly compiled records.

Negative proof kinds:
- adapter-contract-impossible;
- active-probe-recognized-unsupported;
- complete-enumeration-exclusion.

1.0 remains semantically restricted to adapter-contract-impossible.

## Active contract trust boundary

A ProbeReceipt alone does not mean support.

Truth requires:
`registered active contract + exact receipt binding + recognized summary + current basis + PCCR rules`.

There are no production active contracts in WO-009.

## TEST_ONLY fixture model

Test-only contracts bind exact test-only probe descriptor digests.

Supported/unsupported summary values are closed exact strings, not regex/free-form vendor text.

This proves the semantic engine without pretending to know a vendor interface.

## Failure principle

Every non-recognized or failure outcome moves toward UNKNOWN/BLOCKED, never toward FALSE or TRUE.

# M17 — Project Resume Bootstrap
Status: DISCOVERY | Class: NECESSARY

Mission: resume a project/session from Git + UADS sidecar with minimal orientation cost and no dependence on prior chat memory.

Standalone: fully functional alone. Hive complement: can use Hive durable context when connected, but must fall back to repository/sidecar truth.

Candidate technology radar, UNAPPROVED: continuity manifest; delta orientation; source fingerprint cache; stale-resume detector.

Sessions S00–S07 cover restart metrics, bootstrap contract, architecture, stale/cross-project failures, fresh-process tests, implementation, Hive optional path, freeze.

Mandatory tests: fresh process resume, changed checkpoint invalidates cache, no full repo scan unless needed, cross-project replay blocked.

# UADS2-WO-026 — M03 Proven Capability Resolution + Dispatch Adapter Binding — Runtime Evidence Bundle

Status: COMPLETE_CANDIDATE — active-PCCR correction (correction 02) applied on top of the accepted P6 correction revision; P1–P10 proofs, focused/full tests, deterministic evals and this Evidence Bundle are complete on the correction content; exact-head gates and the independent HEDS re-audit are PENDING on the new final head
Issue: #78
Risk: HIGH

## Correction history
- HEDS `5181536976` returned **CORRECTION REQUIRED** on the previously published head `7a4a7ea70b058443442caf0599d3b4f5e6f08796` (PR #80, branch `feat/uads2-wo-026-m03-proven-capability-resolution`); P6 was accepted and the only finding was the missing stored/active PCCR integration proof.
- Defect (correction 02): `resolveHostCapabilityProofs` evaluated **every** stored proof against the passive bridge basis. PCCRs produced by `compileActiveEvidenceToPccr()` are bound to a contract-specific basis (`descriptorDigest`, `policyDigest`, active configuration binding over `{configurationDigest, executableIdentityDigest}`), so a legitimate active E3 SUPPORTED PCCR was rejected as a basis mismatch and stayed UNKNOWN. The frozen Test Plan requirement — a current, basis-matching stored/active PCCR projects TRUE through `readHostCapabilityProjection()` — was therefore not proven end-to-end.
- Correction (correction 02): stored-proof evaluation is source-aware inside M03. A stored proof whose `(probeId, host adapterId, capabilityId)` matches a registered active-evidence contract is evaluated against the current basis derived from that contract plus the supplied current host context through the existing M03 active-evidence API `buildActiveEvidenceCurrentBasis()` (contract adapter/descriptor/policy digests + `activeConfigurationBindingDigest`). Every other stored proof is evaluated against the passive basis exactly as before. Unregistered probe sources, a missing host context, contract/adapter drift, blocked contracts and unusable contracts stay UNKNOWN and non-enabling (`ACTIVE_EVIDENCE_CONTEXT_MISSING`, `ACTIVE_CONTRACT_ADAPTER_MISMATCH`, `TEST_ONLY_ACTIVE_CONTRACT_BLOCKED`, `NO_PRODUCTION_ACTIVE_CONTRACT_IN_WO009`, `ACTIVE_CONTRACT_BASIS_UNAVAILABLE`).
- IF-001 remains the only consumer truth boundary: `readHostCapabilityProjection()` only forwards the optional `activeEvidenceCurrent` context; consumers never learn proof-store/probe internals and all proof-source resolution stays in M03. No CEL/NPC weakening (SUPPORTED evidence floor and NPC semantics unchanged), no schema change (runtime capability snapshot stays 0.8.0, proof schema family untouched), no `requireProvenRuntime` relaxation, no `readRuntimeCapabilitySnapshot` enablement, no Model Lock/M05/M06/M07/M24 ownership change, no new dependency, no network/provider/shell/credential/project-local-state addition.
- Correction 01 history (accepted by this correction, unchanged): HEDS `5180347417` returned **CORRECTION REQUIRED** on `081bcda9888bfed5135ea46b0b6882427bee2c4f`; dispatch without an explicit governed adapter identity was permitted to proceed through a conservative all-UNKNOWN (`host-managed`) capability snapshot instead of hard-blocking. `resolveDispatchRuntimeCapability` (`src/kernel/execution.ts`) now throws `ExecutionBlockedError` with reason `CAPABILITY_TRUTH_ADAPTER_UNSPECIFIED` before any routing whenever the adapter identity is absent — independent of registry size, risk class, host-managed compatibility or legacy runtime state. No implicit adapter inference, no host-managed exception, no UNKNOWN→ALLOW.
- Gate receipts produced on `081bcda`: CI `34614389348`, CodeQL `34614389331`, Dependency Review `34614389145`, Cross-Platform `34614389132` — **INVALIDATED** by the correction revisions (the head moved).
- Gate receipts produced on `7a4a7ea`: CI `34625001237`, CodeQL `34625001327`, Dependency Review `34625001234`, Cross-Platform `34625001343` — **INVALIDATED** by this revision (the head moved); all four are re-run on the new final head.
- PR #77 remains untouched at `5889e0251ce8aa515902a05e1753f5882374bc3c`. No merge has been performed.

## Exact identities
- Base main SHA: `6da568505bbc218273b622ef0ae7d3223ea16182` (verified equal to `origin/main` after `git fetch origin`; branch `feat/uads2-wo-026-m03-proven-capability-resolution` created from / equal to it)
- Context Lock / TEST-PLAN recorded freeze base: `a0a778e5fa4a28750540246fa5894c91a92d0b2b` — controlled source-baseline delta to `6da5685` is docs/governance-only (see "Source baseline verification"; zero runtime/source/schema identity movement)
- Correction 02 commit (current implementation revision): `69c4027ab3bd1f46b13343ba188fd46f88cd21bb` — `fix(UADS2-WO-026): resolve registered active-evidence PCCRs against their contract basis (active PCCR correction 02)` (4 files changed, +400/−12). Supersedes correction 01 commit `047fae18b6210f6d05db43b1b25a70d62c2e0818`, implementation commit `91f08cc70174fffa5b8cab0bf7a3c44fc5a47b91` and rejected heads `081bcda9888bfed5135ea46b0b6882427bee2c4f` / `7a4a7ea70b058443442caf0599d3b4f5e6f08796`.
- Branch diff vs base main `6da5685`: 29 files changed, +1761/−150 (cumulative).
- This Evidence Bundle is committed as the immediately following revision — the resulting final head is a new revision and receives fresh exact-head gates; the final head SHA is recorded in the PR body.
- Node version: `v24.18.0`
- OS/platform: `win32 10.0.26200` (Windows, PowerShell) — developer host; Windows/Linux determinism is additionally covered by the Cross-Platform Compatibility workflow on the exact final head
- Executor/host model routing state: global UADS `v0.12.1` — `uads doctor`: all foundation checks passed (node/git/git-repository/uads-home/global-layout/sidecar-workspace/host-adapters `cursor=SUPPORTED/INSTALLED/CLEAN, codex=SUPPORTED/INSTALLED/CLEAN, generic-agent-skills=SUPPORTED/INSTALLED/CLEAN`); `uads status`: `modelRegistryStatus: valid` (2 profiles), `specialistRegistryStatus: valid` (25 profiles), `zeroProjectFootprint: true`, sidecar workspace not created. No specialist worker was launched for this increment (V2 resource guard: single executor, sequential internal execution).
- Requested effort / applied effort: single Codex Desktop executor session; no effort override recorded by this increment; no paid provider/model calls executed (implementation, fixtures and evals are zero-cost local execution).

## Source baseline verification
- All 26 frozen source identities listed in `.engineering/context-locks/UADS2-WO-026.md` (M03 boundary/substrate, dispatch seam, eval fixtures, schemas, focused tests) were re-verified at branch base `6da5685` with `git rev-parse HEAD:<path>` against the frozen blob list: **0 mismatches / 26 OK**.
- Pre-edit delta `a0a778e..6da5685` (3 commits) is docs/governance-only: `.engineering/checkpoints/CHECKPOINT-DELTA-UADS2-WO-026.md`, `.engineering/context-locks/UADS2-WO-026.md`, `.engineering/plans/UADS2-WO-026-TEST-PLAN.md`, `.engineering/reports/EVIDENCE-UADS2-WO-026-TEMPLATE.md`, `docs/v2/11-CHECKPOINT.md`, `docs/v2/continuity/CURRENT.json`, `docs/v2/planning/UADS2-WO-026-DISPATCH-BINDING.md`, `docs/v2/planning/UADS2-WO-026-M03-PROVEN-CAPABILITY-RESOLUTION-FREEZE.md` (`git diff --name-status a0a778e..6da5685`). No `src/`, `tests/` or `schemas/` movement — no source-baseline delta beyond governance docs.
- Correction 02 stays inside the locked M03 boundary: `git diff --name-status 7a4a7ea..69c4027` is exactly `src/kernel/host-capability-resolver.ts`, `src/adapters/host-capability-consumer.ts` (context forwarding only), `src/kernel/host-capability-active-evidence.ts` (gate extraction + bounded registry lookup) and `tests/host-capability-resolver.test.ts` — no new source file, no schema, no eval fixture, no dispatch/CLI file and no Context Lock dependency drift.

## Changed files
- `src/kernel/host-capability-resolver.ts` (new) — M03 stored-proof resolution behind IF-001: computes the current host subject/adapter-contract/proof basis from the passive projection, resolves stored PCCR only through M03 read/evaluate/project primitives, and returns a per-capability contribution table (`stored-proof` vs `passive`, `proofSource` `passive-contract`/`active-evidence`/null, `supported`/`unsupported`/`unknown`, bounded reason codes) plus `proofBacked`. **Correction 02:** evaluation is source-aware — a stored proof matching a registered active-evidence contract by `(probeId, host adapterId, capabilityId)` is evaluated against the current basis derived from that contract plus the supplied current host context; every other stored proof keeps the passive basis; unregistered sources, a missing context, adapter drift, blocked contracts and unusable contracts are non-enabling. Read-only; never writes proof state; never throws for missing/rejected/stale/mismatched/expired/clock-regressed evidence (those stay UNKNOWN and non-enabling).
- `src/kernel/host-capability-active-evidence.ts` — M03 active-evidence substrate. **Correction 02:** extracted `activeEvidenceContractBlockReason()` (single availability/TEST_ONLY gate, now shared by `compileActiveEvidenceToPccr()` and the resolver with the same statuses/reason codes) and added `findRegisteredActiveEvidenceContract({probeId, adapterId, capabilityId})` (bounded lookup over the existing registered/versioned contract registry; returns null when absent or ambiguous). Registry, contracts, receipts, parser, lease and privacy semantics unchanged.
- `src/adapters/host-capability-consumer.ts` — facade evolution: `readHostCapabilityProjection()` now resolves current basis-bound stored proof through the resolver; TRUE only from current valid SUPPORTED proof at the required evidence class; FALSE only from current valid NPC-compliant UNSUPPORTED; everything else UNKNOWN; provenance `proven` only when PCCR-backed by validated evidence for this read; passive/legacy/adapter-declared TRUE stays UNKNOWN. **Correction 02:** also accepts the optional `activeEvidenceCurrent` host context and forwards it to the resolver; no resolution logic is added to the adapter layer.
- `src/kernel/execution.ts` — dispatch adapter identity / plan capability input: `resolveDispatchRuntimeCapability` accepts explicit adapter identity as governed input, validates it against actual host detection/subject identity and blocks visibly on unknown/blocked identity (`CAPABILITY_TRUTH_ADAPTER_UNKNOWN:<id>` / `CAPABILITY_TRUTH_ADAPTER_BLOCKED`). **Correction 01 (accepted):** an absent identity hard-blocks with `ExecutionBlockedError` reason `CAPABILITY_TRUTH_ADAPTER_UNSPECIFIED` before any routing — the previous conservative all-UNKNOWN (`host-managed`) resolution is gone from this seam, at any registry size, risk class or legacy runtime state; adapter-bound dispatch still forces the generic runtime identity.
- `src/commands/dispatch.ts` — command layer threads the explicit adapter identity into `runDispatch`.
- `src/cli.ts` — CLI entry passes the explicit adapter identity through to the dispatch command.
- `src/eval/execution.ts` — X7 fixture migration: proven legacy runtime-snapshot seeding replaced by deterministic PCCR proof fixtures compiled/persisted through public M03 APIs under the test environment (same public/current basis inputs the production facade evaluates). Correction 01: every dispatch call site carries the explicit governed `adapterId`.
- `src/eval/fault-injection-normative.ts` — FI4/FI5/FI8/FI9/FI10 fixture migration: same public-API PCCR proof seeding; no legacy snapshot enablement and no `requireProvenRuntime` relaxation. Correction 01: the dispatch call sites pass `adapterId` unconditionally (conditional spreads removed).
- `src/eval/context.ts`, `src/eval/cost.ts`, `src/eval/fault.ts` — legacy dispatch call sites updated to carry the explicit governed `adapterId`.
- `tests/host-capability-resolver.test.ts` (new, 19 tests) — P1–P4/P9 resolution proofs. **Correction 02:** +7 tests = the active-evidence E2E (registered TEST_ONLY contract → bounded successful receipt → `compileActiveEvidenceToPccr()` → `persistHostCapabilityProof()` → `readHostCapabilityProjection()` = TRUE + `proven` with contribution `proofSource=active-evidence`, plus an explicit pre-correction passive-basis counter-proof that the same stored PCCR is STALE under the passive basis) and the active negatives (missing host context, expired lease, probe-definition/descriptor drift, policy drift, configuration drift on both host context and proof binding, subject drift, adapter drift on both proof and host context, unregistered probe source).
- `tests/host-dispatch-adapter-binding.test.ts` (9 tests) — P5×4 / P6×3 / P7×2 dispatch binding proofs (incl. static import-graph checks).
- `tests/cli-smoke.test.ts` — CLI negative proof: `dispatch` without an adapter identity exits non-zero with `explicit governed adapter identity` on stderr (guarded by the existing `dist/cli.js` presence skip).
- `tests/host-execution.test.ts`, `tests/cache-cost.test.ts`, `tests/execution-correction-01.test.ts`, `tests/execution-correction.test.ts`, `tests/execution-digest.test.ts`, `tests/execution-gates.test.ts`, `tests/execution-happy.test.ts`, `tests/execution-lifecycle.test.ts`, `tests/execution-reviewers.test.ts`, `tests/execution-scope.test.ts`, `tests/execution-security.test.ts`, `tests/fault-correction-01.test.ts`, `tests/fault-correction-02.test.ts`, `tests/specialist-dispatch-correction-01.test.ts` — legacy dispatch fixtures updated to pass an explicit governed adapter identity (the previous implicit path is now an enforced block).
- `.engineering/reports/EVIDENCE-UADS2-WO-026.md` (this bundle; updated and committed as the final head — the resulting final head is a new revision and receives fresh exact-head gates + HEDS).

No `schemas/` change: runtime capability snapshot contract stays `0.8.0`; proof schema family unchanged. No `requireProvenRuntime` change. PR #77 untouched at `5889e0251ce8aa515902a05e1753f5882374bc3c`.

## Proof results
- P1 current SUPPORTED projects TRUE / passive TRUE stays UNKNOWN: **PASS** — `tests/host-capability-resolver.test.ts` "P1 projects TRUE only from a current valid SUPPORTED PCCR and marks provenance proven" + "P1 never enables a passive declaration or legacy snapshot TRUE without PCCR evidence". **Active end-to-end (correction 02):** "P1-ACTIVE proves TRUE through a registered active-evidence PCCR and the stored-proof pipeline" runs the registered TEST_ONLY contract `test.active.tool-supported.v1` (probe `test.active-supported.v1`, adapter `generic-agent-skills`, capability `toolCalling`, parser `echo-safe-v1`, LEASED E3) end-to-end: bounded successful probe receipt → `compileActiveEvidenceToPccr()` → `persistHostCapabilityProof()` → `readHostCapabilityProjection()` → `toolCalling=TRUE` and `provenance.confidence=proven`, with the resolution table reporting `origin=stored-proof`, `proofSource=active-evidence`, `outcome=supported`. The same suite asserts the counter-proof: the stored PCCR is STALE (`PROBE_DEFINITION_DIGEST_MISMATCH`) when evaluated against the pre-correction passive basis.
- P2 missing/stale/expired/corrupt/subject-mismatch/basis-mismatch/clock-regression stays UNKNOWN: **PASS** — resolver suite P2 tests (missing; record-stale; lease-expired; corrupt file without consumer crash; subject mismatch; basis mismatches incl. policy + runtime version; clock regression). **Active negatives (correction 02):** "P1-ACTIVE keeps a stored active proof UNKNOWN and non-enabling without a current host context" and "P2-ACTIVE rejects an expired active lease / probe-definition and policy drift / configuration binding drift / subject and adapter drift as UNKNOWN" plus "P2-ACTIVE never grants active trust to an unregistered probe source" — every case asserts the capability stays `unknown`, provenance `unknown` and the contribution stays `passive`/`proofSource=null` (never `proven`), with the bounded evaluator/routing reason codes (`LEASE_EXPIRED`, `PROBE_DEFINITION_DIGEST_MISMATCH`, `POLICY_DIGEST_MISMATCH`, `CONFIGURATION_DIGEST_MISMATCH`, `SUBJECT_DIGEST_MISMATCH`, `ADAPTER_ID_MISMATCH`, `ACTIVE_CONTRACT_ADAPTER_MISMATCH`, `ACTIVE_EVIDENCE_CONTEXT_MISSING`).
- P3 valid NPC UNSUPPORTED projects FALSE / invalid negative proof cannot: **PASS** — resolver suite "P3 projects FALSE from a current NPC-compliant UNSUPPORTED proof and never from invalid ones" (E2 floor and the PCCR 1.0 `adapter-contract-impossible` requirement still enforced by `compileHostCapabilityProof`).
- P4 passive-only truth stays non-proven and per-capability independence: **PASS** — resolver suite "P4 keeps passive-only truth non-proven and per-capability independence" (a capability with no stored proof keeps the passive value, provenance `unknown`, contribution `origin=passive`). **Correction 02:** the active E2E additionally asserts that a capability without active evidence keeps `origin=passive, proofSource=null`, and "P2-ACTIVE never grants active trust to an unregistered probe source" proves the discriminator is bound to the registered contract (arbitrary probeId values do not gain active trust; the contribution remains passive/non-enabling).
- P5 dispatch binds explicit governed adapter identity: **PASS** — dispatch-binding suite P5 tests (adapterId recorded, `generic-runtime` runtime identity, no proof writes; mismatched/ungoverned identity visibly blocked; detection-BLOCKED → `CAPABILITY_TRUTH_ADAPTER_BLOCKED`).
- P6 dispatch without adapter identity hard-blocks before routing, independent of registry/risk/host-managed compatibility/legacy state: **PASS** — dispatch-binding suite "P6 blocks absent adapter identity before routing when profiles and legacy state exist" (valid model registry + legacy runtime snapshot present), "P6 blocks absent adapter identity and persists no runtime state when no profiles exist", "P6 blocks the dispatch command without adapter identity and does not advance" (command layer); each asserts `ExecutionBlockedError` code `CAPABILITY_TRUTH_ADAPTER_UNSPECIFIED` and **zero** execution-run artifacts persisted. CLI negative: `tests/cli-smoke.test.ts` asserts `dispatch --session imp-1` exits 1 with `explicit governed adapter identity` on stderr. Unchanged by correction 02.
- P7 consumers use IF-001 only; no raw proof-store/probe coupling: **PASS** — dispatch-binding suite "P7 keeps M05 dispatch on the IF-001 facade only" + "P7 confines proof-store/probe imports to the M03 boundary and authorized fixtures" (static import-graph assertions). Correction 02 keeps `readHostCapabilityProjection()` as the only consumer surface (the adapter forwards a typed context; no proof-store/probe import was added to any consumer).
- P8 no paid/model call by default; probes bounded, auditable, privacy-safe: **PASS** — no network/shell primitives in resolver/consumer/proof/active-evidence modules (`rg` check, zero hits); proof resolution performs zero model-bearing calls by construction (no provider imports); the probe used by the active E2E is the bounded, registered TEST_ONLY descriptor (`READ_ONLY_LOCAL`, network `DENY`, 1000ms timeout, 64-byte output budget, fixed args); probe policy unchanged; existing privacy assertions (no host-path leakage) remain in force and are re-exercised by the P9 path-safety test.
- P9 Windows/Linux deterministic identity and proof evaluation: **PASS (local Windows)** — resolver suite "P9 keeps subject, basis and projection identity deterministic and path-safe"; the active basis is a pure digest of the registered contract + supplied host context (no clock/locale/OS input). Linux determinism is proven by the Cross-Platform Compatibility workflow on the exact final head (receipt captured on the final head via the Cross-Platform Compatibility workflow and returned to HEDS with the PR).
- P10 PR #77 X7/FI regressions removable without legacy enablement or weakened requirement: **PASS** — `node dist/eval/execution.js` → **9 passed / 0 failed** (X7 recovered); `node dist/eval/fault-injection.js` → **32 passed / 0 failed** (FI4/FI5/FI8/FI9/FI10 recovered) with fixtures seeded through public M03 APIs, no legacy snapshot enablement, `requireProvenRuntime` untouched.
- M05 RT/ES (RT-001/002/003/009/010/011, ES-013/014), M06 (RT-004..008), M07/M24 families, M03 V1 proofs: **NOT_APPLICABLE** (owners: resumed IW1-01 / IW1-02 / IW1-03 / IW1-04; frozen V1 module — per `.engineering/plans/UADS2-WO-026-TEST-PLAN.md` out-of-scope list).

## Tests
### Focused
Command: `npx vitest run --maxWorkers=1 tests/host-capability-consumer.test.ts tests/host-capability-proof.test.ts tests/host-capability-resolver.test.ts tests/host-capability-passive.test.ts tests/host-capability-active-evidence.test.ts tests/host-capability-probe.test.ts tests/host-capability-cross-platform.test.ts tests/host-dispatch-adapter-binding.test.ts tests/host-dispatch-proof-aware.test.ts`
Result: **9 files / 153 tests / 153 passed / 0 failed**, exit 0, duration 72.30s (tests 64.69s) — run at the correction 02 content; `git diff 69c4027 -- src tests` is clean, so the committed tree is byte-identical to the tested tree. P5×4 + P6×3 + P7×2 included. Resolver file in isolation: 19/19 (12 existing + 7 new active-evidence tests).

### Full suite
Command: `npm test` (`vitest run --maxWorkers=1`)
Result: **1 failed | 61 passed (62) files; 1 failed | 604 passed (605) tests; duration 1811.95s (30.2 min)**, exit 1 (vitest exit reflects the single pre-existing failure). The single failure is `tests/release-security-proof.test.ts > RG1-RG25 release security proof > RG14 keeps v0.11.0 outside corrected-release proof semantics and immutable` — pre-existing and unrelated: the test requires remote tag `refs/tags/v0.11.0` (`git ls-remote origin refs/tags/v0.11.0` returned empty in this clone; expected `d5cb361274cb19f70c8bd02dd023b596b8babf13`). Same sole failure as the pre-correction runs (1 failed | 597 passed (598) at `7a4a7ea`, 1 failed | 595 passed (596) at `081bcda`); 605 = 598 + 7 new active-evidence tests, so **zero new failures**; the WO-007 p95 benchmark in `tests/host-capability-passive.test.ts` passed in this full run.

### Build / typecheck / lint
- `npm run build` (`tsc -p tsconfig.json`): exit 0 on the correction content (dist rebuilt before the eval batch; `dist/` is not committed).
- `npm run typecheck` / `npm run lint` (`tsc -p tsconfig.json --noEmit`): exit 0 after the correction edits (both commands are the same invocation).

## Deterministic evals
All ten CI evals ran on the correction content with `dist/` built from it (all exit 0):
- `node dist/eval/run.js` → 9 passed / 0 failed
- `node dist/eval/execution.js` → 9 passed / 0 failed (X7 recovered)
- `node dist/eval/context.js` → 19 passed / 0 failed
- `node dist/eval/fault.js` → 18 passed / 0 failed
- `node dist/eval/cost.js` → 27 passed / 0 failed
- `node dist/eval/model-routing.js` → 22 passed / 0 failed
- `node dist/eval/specialist-routing.js` → 26 passed / 0 failed
- `node dist/eval/adapters.js` → 40 passed / 0 failed
- `node dist/eval/assurance.js` → 22 passed / 0 failed
- `node dist/eval/fault-injection.js` → 32 passed / 0 failed (FI4/FI5/FI8/FI9/FI10 recovered)

## Runtime scenarios
- current stored SUPPORTED passive proof resolution → TRUE + `proven`: resolver P1.
- registered active-evidence PCCR (TEST_ONLY contract) → TRUE + `proven` through the stored-proof pipeline: resolver P1-ACTIVE (correction 02).
- stored active proof presented without the current host context → UNKNOWN/non-enabling: resolver P1-ACTIVE negative half (correction 02).
- expired active lease / probe-definition drift / policy drift / configuration drift / subject drift / adapter drift on an active proof → UNKNOWN: resolver P2-ACTIVE tests (correction 02).
- unregistered probe source presenting an active-style basis → UNKNOWN, contribution stays passive: resolver P2-ACTIVE test (correction 02).
- passive-only fallback projection (provenance `unknown`): resolver P4 + existing consumer boundary suite.
- passive TRUE declaration (no PCCR evidence) → UNKNOWN: resolver P1 (negative half).
- missing proof / stale proof / expired lease / corrupt proof file: resolver P2 tests (corrupt case asserts no consumer crash; UNKNOWN outcome).
- subject mismatch / runtime-version basis mismatch / adapter-contract & probe/policy/configuration basis mismatch / clock regression: resolver P2 tests.
- non-NPC negative proof: resolver P3 negative half.
- dispatch with explicit adapter identity (valid): dispatch-binding P5 tests (adapterId recorded, `generic-runtime` runtime identity, no proof writes).
- dispatch without adapter identity: dispatch-binding P6 tests — hard block `ExecutionBlockedError` `CAPABILITY_TRUTH_ADAPTER_UNSPECIFIED` before routing, no persisted runtime state, no execution-run artifacts, even with a valid registry and a legacy runtime snapshot present; CLI negative in `cli-smoke`.
- dispatch with mismatched/ungoverned adapter identity: dispatch-binding P5 tests (visible block; never inferred from legacy state); detection-BLOCKED identity → `CAPABILITY_TRUTH_ADAPTER_BLOCKED`.
- no model-bearing calls during proof resolution: by construction (no provider/network imports in resolver/consumer/proof/active-evidence modules; `rg` evidence above); no paid provider/model call executed anywhere in this increment.

## Performance observation
Environment: developer host `win32 10.0.26200`, Node `v24.18.0`, local sidecar proof store; no paid benchmark; not a production SLO/capacity claim.
- Focused 9-file re-run at the correction 02 content: 153 tests in 72.30s (tests 64.69s; includes fixture setup/teardown and the active probe spawns).
- Full suite at the correction 02 content: 605 tests in 1811.95s (30.2 min).
- Stored-proof resolution is in-process, single-pass file reads of bounded JSON + schema validation; the active path adds one in-memory registry lookup by `(probeId, adapterId, capabilityId)` and one digest of the supplied host context per stored active proof — no model-bearing calls, no network, no unbounded probes; no per-call latency instrumentation was added (out of scope for this increment).

## Security/privacy
- secret/host-path leakage into proof/projection artifacts: no hits in projection artifacts; P9 path-safety test asserts projection JSON carries `path.basename(host)` only, never absolute host paths; no secrets introduced.
- bounded reason codes / no raw provider payloads: resolver returns fixed-set reason codes (`PROOF_MISSING`, `PROOF_REJECTED`, evaluator codes such as stale/lease/clock semantics, active routing codes `ACTIVE_EVIDENCE_CONTEXT_MISSING`, `ACTIVE_CONTRACT_ADAPTER_MISMATCH`, `TEST_ONLY_ACTIVE_CONTRACT_BLOCKED`, `NO_PRODUCTION_ACTIVE_CONTRACT_IN_WO009`, `ACTIVE_CONTRACT_BASIS_UNAVAILABLE`, `CAPABILITY_TRUTH_ADAPTER_UNKNOWN:<id>`/`_BLOCKED`, `CAPABILITY_TRUTH_ADAPTER_UNSPECIFIED`); no raw payload echo.
- fail-closed dispatch seam: absent/mismatched adapter identity cannot be silently upgraded to an executable plan (no UNKNOWN→ALLOW; P6 asserts no execution-run artifacts).
- fail-closed active recognition: trust in an active PCCR comes only from a registered/versioned contract matched by `(probeId, adapterId, capabilityId)` plus a supplied current host context; unregistered probe IDs, missing context and contract/adapter drift are non-enabling (correction 02 tests).
- closed schemas: no schema files changed; existing `additionalProperties: false` families unchanged (`schemas/host-capability-proof.schema.json`, `schemas/runtime-capability-snapshot.schema.json` blobs verified identical to the frozen baseline).
- no project-local UADS state; global sidecar only: `uads status` → `zeroProjectFootprint: true`, `workspaceExists: false`; proof fixtures use the global sidecar layout under the test environment (or isolated temp homes for tests).
- no arbitrary shell / unbounded probes / vendor credentials: no `child_process`/shell/network primitives in the changed modules (`rg` check); probe registry policy unchanged; the E2E uses the existing bounded registered TEST_ONLY descriptor.

## Repository gates on exact final head
- CI: PENDING — exact-head receipt is captured on the new final head via the PR; the receipts from `7a4a7ea` (run 34625001237) and `081bcda` (run 34614389348) are INVALIDATED by this revision
- CodeQL: PENDING — same as CI (receipt 34625001327 on `7a4a7ea` and 34614389331 on `081bcda` invalidated)
- Dependency Review: PENDING — same as CI (receipt 34625001234 on `7a4a7ea` and 34614389145 on `081bcda` invalidated)
- Cross-Platform: PENDING — same as CI (receipt 34625001343 on `7a4a7ea` and 34614389132 on `081bcda` invalidated)
Any new commit after these receipts invalidates the exact-head claims above.

## HEDS
Prior review: `5181536976` — **CORRECTION REQUIRED** on `7a4a7ea70b058443442caf0599d3b4f5e6f08796` (historical; the missing stored/active PCCR integration proof is corrected in this revision and its receipt is not a current approval)
Earlier review: `5180347417` — **CORRECTION REQUIRED** on `081bcda9888bfed5135ea46b0b6882427bee2c4f` (historical; P6 defect corrected and accepted in correction 01)
Re-audit: PENDING — independent final audit runs on the new final head after exact-head gates
Verdict: PENDING

## Final verdict
COMPLETE_CANDIDATE — active-PCCR correction revision + complete Evidence Bundle at the final head; exact-head CI/CodeQL/Dependency Review/Cross-Platform receipts and the independent HEDS re-audit remain the outer gate (the COMPLETE_CANDIDATE return to HEDS is made only after all four exact-head gates report SUCCESS). No merge.

Do not mark APPROVED while any release-blocking capability-truth, provenance, basis-matching, adapter-identity, exact-head gate or privacy requirement is FAIL/BLOCKED.